# GigShield v6 — Python 3.10+ (3.13 compatible)
fastapi==0.115.5
uvicorn==0.32.1
sqlalchemy==2.0.36
httpx==0.28.0
pydantic==2.10.3
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1

# ML — install separately if needed:
# pip install scikit-learn numpy
# (system will use rule-based fallback if not installed)
scikit-learn>=1.3.0
numpy>=1.24.0
